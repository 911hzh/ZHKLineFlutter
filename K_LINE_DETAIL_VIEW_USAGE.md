# K 线详情视图使用说明

## 功能概述

K 线详情视图 (`KLineDetailView`) 是一个浮层组件，在用户长按或点击 K 线时显示详细信息。

## 显示效果

当用户长按或点击 K 线时，会在屏幕左上角或右上角显示一个浮层，包含以下信息：

```
2025-10-16
开    123.45
高    125.67
低    121.23
收    124.56
涨跌额    +1.11
涨跌幅    +0.90%
成交量    1234567.89
```

## 交互行为

### 显示时机

- **长按**：长按 K 线时显示详情视图
- **长按移动**：长按移动时，详情视图会更新为当前位置的 K 线数据
- **点击**：单击 K 线时也会显示详情视图
- **保持显示**：长按结束后，详情视图保持显示（配合十字线）

### 隐藏时机

- **滚动**：滚动图表时自动隐藏
- **缩放**：缩放时会保持显示（如需隐藏可自行修改）

### 位置逻辑

- 触摸点在屏幕**左半部分**：详情视图显示在**右上角**
- 触摸点在屏幕**右半部分**：详情视图显示在**左上角**
- 这样可以避免详情视图遮挡用户查看的内容

## 视觉样式

### 背景和边框

- 背景色：十字线颜色 + 90% 不透明度
- 边框：1px 实线，使用十字线颜色
- 圆角：6px
- 内边距：水平 6px，垂直 3px

### 文字样式

- 字体大小：8px
- 字体颜色：黑色（默认）
- 涨跌额/涨跌幅：根据正负显示绿色（涨）或红色（跌）

### 动画效果

- 显示/隐藏：200ms 渐变动画

## 代码实现

### 核心组件

#### KLineDetailView

```dart
KLineDetailView(
  data: selectedKLineModel,  // 要显示的 K 线数据
  preferRight: true,          // 是否优先显示在右侧
)
```

#### 在 KLineView 中的集成

详情视图已经集成在 `KLineView` 中，无需额外配置。当用户长按或点击时会自动显示。

### 状态管理

在 `_KLineViewState` 中维护以下状态：

- `_selectedKLineModel`: 当前选中的 K 线数据
- `_shouldShowCrossLine`: 是否显示十字线和详情视图
- `_crossLinePoint`: 十字线位置（含滚动偏移）
- `_touchScreenPoint`: 触摸点的屏幕坐标（用于判断显示位置）

## 时区说明

时间显示使用新加坡时区（UTC+8），格式为 `yyyy-MM-dd`。

实现方式：

```dart
final date = DateTime.fromMillisecondsSinceEpoch(data.timestamp);
final formatter = DateFormat('yyyy-MM-dd');
final timeString = formatter.format(date.toUtc().add(const Duration(hours: 8)));
```

## 数据格式

### 价格和成交量

- 保留两位小数
- 格式：`xxx.xx`

### 涨跌额

- 正数显示 `+` 号
- 格式：`+/-xxx.xx`
- 颜色：涨绿跌红

### 涨跌幅

- 正数显示 `+` 号
- 格式：`+/-xx.xx%`
- 颜色：涨绿跌红

## 自定义修改

### 修改显示内容

编辑 `k_line_detail_view.dart` 中的 `build` 方法，添加或删除要显示的数据项。

### 修改样式

编辑 `k_line_detail_view.dart` 中的 `BoxDecoration` 和 `TextStyle`。

### 修改位置逻辑

编辑 `k_line_view.dart` 中的 `_buildDetailView` 方法，修改 `preferRight` 的判断逻辑。

### 修改显示时机

编辑 `k_line_view.dart` 中的手势处理方法：

- `_onTapUp`: 点击时显示
- `_onLongPressStart`: 长按开始时显示
- `_onLongPressMoveUpdate`: 长按移动时更新
- `_onLongPressEnd`: 长按结束时的行为
- `_onScroll`: 滚动时的行为

## 注意事项

1. **性能优化**：详情视图使用 `IgnorePointer` 包裹，不会拦截手势事件
2. **动画性能**：使用 `AnimatedOpacity` 实现渐变，性能优于手动动画
3. **依赖项**：需要 `intl` 包进行日期格式化（已在 `pubspec.yaml` 中）
4. **坐标系统**：注意区分屏幕坐标和内容坐标（含滚动偏移）

## 与 Swift 版本的对比

| 特性     | Swift | Flutter | 说明     |
| -------- | ----- | ------- | -------- |
| 显示内容 | ✅    | ✅      | 完全一致 |
| 智能定位 | ✅    | ✅      | 完全一致 |
| 时区处理 | ✅    | ✅      | 完全一致 |
| 动画效果 | ✅    | ✅      | 完全一致 |
| 手势支持 | ✅    | ✅      | 完全一致 |
| 滚动隐藏 | ✅    | ✅      | 完全一致 |

## 测试建议

1. **长按测试**：长按 K 线，检查详情视图是否正确显示
2. **移动测试**：长按后移动手指，检查详情视图数据是否更新
3. **位置测试**：分别在屏幕左侧和右侧长按，检查详情视图位置是否正确
4. **滚动测试**：显示详情视图后滚动，检查是否自动隐藏
5. **数据测试**：检查显示的数据是否正确（特别是涨跌额和涨跌幅）
6. **动画测试**：检查显示/隐藏动画是否流畅

## 故障排查

### 详情视图不显示

1. 检查 `_shouldShowCrossLine` 是否为 `true`
2. 检查 `_selectedKLineModel` 是否不为 `null`
3. 检查手势是否被其他组件拦截

### 位置不正确

1. 检查 `_touchScreenPoint` 是否正确保存
2. 检查 `preferRight` 的判断逻辑

### 数据显示错误

1. 检查 `KLineModel` 数据是否正确
2. 检查时间戳格式（应为毫秒）
3. 检查涨跌额和涨跌幅的计算

### 动画不流畅

1. 检查是否频繁调用 `setState`
2. 考虑使用 `RepaintBoundary` 优化渲染
